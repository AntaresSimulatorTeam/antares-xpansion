diff -ruN a/Clp/src/ClpModel.cpp b/Clp/src/ClpModel.cpp
--- a/Clp/src/ClpModel.cpp	2026-08-24 19:04:19.277320028 +0200
+++ b/Clp/src/ClpModel.cpp	2026-08-24 19:04:19.297320155 +0200
@@ -1402,6 +1402,11 @@
 {
   if (!number)
     return; // nothing to do
+#ifdef CLP_DIAG_INSTRUMENTATION
+  fprintf(stderr, "[CLP-DIAG] ClpModel::deleteRows this=%p deleting %d rows: numberRows_ before=%d problemStatus_ before=%d\n",
+    (void *)this, number, numberRows_, problemStatus_);
+  fflush(stderr);
+#endif
   whatsChanged_ &= ~(1 + 2 + 4 + 8 + 16 + 32); // all except columns changed
   int newSize = 0;
 #define CLP_TIDY_DELETE_ROWS
@@ -1772,6 +1777,11 @@
   if (number) {
     whatsChanged_ &= ~(1 + 2 + 8 + 16 + 32); // all except columns changed
     int numberRowsNow = numberRows_;
+#ifdef CLP_DIAG_INSTRUMENTATION
+    fprintf(stderr, "[CLP-DIAG] ClpModel::addRows this=%p adding %d rows: numberRows_ %d -> %d (numberColumns_=%d) problemStatus_ before=%d\n",
+      (void *)this, number, numberRowsNow, numberRowsNow + number, numberColumns_, problemStatus_);
+    fflush(stderr);
+#endif
     resize(numberRowsNow + number, numberColumns_);
     double *lower = rowLower_ + numberRowsNow;
     double *upper = rowUpper_ + numberRowsNow;
diff -ruN a/Clp/src/ClpNonLinearCost.cpp b/Clp/src/ClpNonLinearCost.cpp
--- a/Clp/src/ClpNonLinearCost.cpp	2026-08-24 19:04:19.201319546 +0200
+++ b/Clp/src/ClpNonLinearCost.cpp	2026-08-24 19:04:19.217319647 +0200
@@ -67,6 +67,11 @@
   //model_->setMaximumIterations(30000);
   //}
   numberColumns_ = model_->numberColumns();
+#ifdef CLP_DIAG_INSTRUMENTATION
+  fprintf(stderr, "[CLP-DIAG] new ClpNonLinearCost %p for model %p: built numberRows_=%d numberColumns_=%d (algorithm_=%d)\n",
+    (void *)this, (void *)model_, numberRows_, numberColumns_, model_->algorithm());
+  fflush(stderr);
+#endif
   // If gub then we need this extra
   int numberExtra = model_->numberExtraRows();
   if (numberExtra)
@@ -1759,6 +1764,19 @@
 int ClpNonLinearCost::setOneOutgoing(int iSequence, double &value)
 {
   assert(model_ != NULL);
+#ifdef CLP_DIAG_INSTRUMENTATION
+  {
+    int builtTotal = numberRows_ + numberColumns_;
+    int liveTotal = model_->numberRows() + model_->numberColumns();
+    if (iSequence >= builtTotal || builtTotal != liveTotal) {
+      fprintf(stderr,
+        "[CLP-DIAG] setOneOutgoing MISMATCH this=%p iSequence=%d | built numberRows_=%d numberColumns_=%d (total=%d) | live model numberRows()=%d numberColumns()=%d (total=%d)\n",
+        (void *)this, iSequence, numberRows_, numberColumns_, builtTotal,
+        model_->numberRows(), model_->numberColumns(), liveTotal);
+      fflush(stderr);
+    }
+  }
+#endif
   double primalTolerance = model_->currentPrimalTolerance();
   // difference in cost
   double difference = 0.0;
diff -ruN a/Clp/src/ClpSimplex.cpp b/Clp/src/ClpSimplex.cpp
--- a/Clp/src/ClpSimplex.cpp	2026-08-24 19:04:19.237319775 +0200
+++ b/Clp/src/ClpSimplex.cpp	2026-08-24 19:04:19.257319901 +0200
@@ -3359,6 +3359,12 @@
   }
   int numberRows2 = numberRows_ + numberExtraRows_;
   int numberTotal = numberRows2 + numberColumns_;
+#ifdef CLP_DIAG_INSTRUMENTATION
+  fprintf(stderr, "[CLP-DIAG] createRim this=%p what=%d newArrays=%d oldMatrix=%d problemStatus_=%d numberRows_=%d numberColumns_=%d nonLinearCost_=%p algorithm_=%d startFinishOptions=%d\n",
+    (void *)this, what, (int)newArrays, (int)oldMatrix, problemStatus_, numberRows_, numberColumns_,
+    (void *)nonLinearCost_, algorithm_, startFinishOptions);
+  fflush(stderr);
+#endif
   if ((specialOptions_ & 65536) != 0) {
     assert(!numberExtraRows_);
     if (!cost_ || numberRows2 > maximumInternalRows_ || numberColumns_ > maximumInternalColumns_) {
@@ -9140,6 +9146,12 @@
     whatsChanged_ &= ~0xffff;
   }
   double saveObjValue = objectiveValue_;
+#ifdef CLP_DIAG_INSTRUMENTATION
+  fprintf(stderr, "[CLP-DIAG] finish this=%p startFinishOptions=%d problemStatus_=%d getRidOfData=%d numberRows_=%d numberColumns_=%d nonLinearCost_=%p saveStatus_=%p\n",
+    (void *)this, startFinishOptions, problemStatus_, getRidOfData, numberRows_, numberColumns_,
+    (void *)nonLinearCost_, (void *)saveStatus_);
+  fflush(stderr);
+#endif
   deleteRim(getRidOfData);
   if (matrix_->type() >= 15)
     objectiveValue_ = saveObjValue;
@@ -12582,6 +12594,12 @@
 // Resizes rim part of model
 void ClpSimplex::resize(int newNumberRows, int newNumberColumns)
 {
+#ifdef CLP_DIAG_INSTRUMENTATION
+  fprintf(stderr, "[CLP-DIAG] ClpSimplex::resize CALLED this=%p %d->%d rows, %d->%d cols, saveStatus_=%p, nonLinearCost_=%p\n",
+    (void *)this, numberRows_, newNumberRows, numberColumns_, newNumberColumns,
+    (void *)saveStatus_, (void *)nonLinearCost_);
+  fflush(stderr);
+#endif
   ClpModel::resize(newNumberRows, newNumberColumns);
   delete[] perturbationArray_;
   perturbationArray_ = NULL;
